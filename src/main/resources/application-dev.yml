# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jiaoyi_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    com.jiaoyi: DEBUG
    org.springframework.web: DEBUG
