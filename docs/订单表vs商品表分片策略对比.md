# 订单表 vs 商品表分片策略对比

## 📊 核心差异总结

| 维度 | 商品表（product-service） | 订单表（order-service） |
|------|---------------------------|------------------------|
| **分片键** | `product_shard_id` | `store_id` |
| **路由方式** | ✅ **路由表查询**（`product_shard_bucket_route`） | ❌ **纯函数取模**（`shard_id % 3`） |
| **扩容支持** | ✅ **支持动态扩容**（只需修改路由表） | ❌ **不支持动态扩容**（需要修改代码） |
| **虚拟桶** | ✅ 固定1024个桶，通过路由表映射 | ✅ 固定1024个桶，但直接取模 |
| **路由缓存** | ✅ `ProductRouteCache`（内存缓存） | ❌ 无缓存（纯函数计算） |
| **降级机制** | ✅ 路由表不可用时降级为取模 | ❌ 无降级机制 |

---

## 🔍 详细对比

### 1. 分片键差异

#### 商品表（product-service）

**分片键**：`product_shard_id`（Integer，0-1023）

```java
// 实体类字段
private Integer productShardId;  // 0-1023

// ShardingSphere 配置
tableRule.setDatabaseShardingStrategy(
    new StandardShardingStrategyConfiguration("product_shard_id", "product_shard_id_database")
);
```

**计算方式**：
```java
product_shard_id = ProductShardUtil.calculateProductShardId(storeId);
// hash(store_id) & 1023
```

#### 订单表（order-service）

**分片键**：`store_id`（Long）

```java
// 实体类字段
private Long storeId;

// ShardingSphere 配置
tableRule.setDatabaseShardingStrategy(
    new StandardShardingStrategyConfiguration("store_id", "store_id_database")
);
```

**计算方式**：
```java
// 在分片算法内部计算
shard_id = ShardUtil.calculateShardId(storeId);
// hash(store_id) & 1023
```

---

### 2. 路由算法差异

#### 商品表：路由表方案（V2版本）

**数据库路由**：`ProductShardIdDatabaseShardingAlgorithmV2`

```java
// 1. 从路由表查询
ds_name = routeCache.getDataSourceName(product_shard_id);
// 查询 product_shard_bucket_route 表

// 2. 降级为取模（如果路由表不可用）
ds_index = product_shard_id % 3;
ds_name = "ds" + ds_index;
```

**表路由**：`ProductShardIdTableShardingAlgorithmV2`

```java
// 1. 从路由表查询
tbl_id = routeCache.getTableId(product_shard_id);
table_name = logic_table + "_" + String.format("%02d", tbl_id);

// 2. 降级为取模（如果路由表不可用）
tbl_id = product_shard_id % 32;
table_name = logic_table + "_" + String.format("%02d", tbl_id);
```

**路由表结构**：`product_shard_bucket_route`
```sql
CREATE TABLE product_shard_bucket_route (
    bucket_id INT PRIMARY KEY,        -- 0-1023
    ds_name VARCHAR(32) NOT NULL,     -- ds0/ds1/ds2
    tbl_id INT NOT NULL,              -- 0-31
    status VARCHAR(16),               -- NORMAL/MIGRATING
    version BIGINT,
    target_ds_id VARCHAR(32),         -- 迁移目标
    target_tbl_id INT,                -- 迁移目标表
    ...
);
```

**路由缓存**：`ProductRouteCache`
- 启动时全量加载（1024条）
- 定时刷新（每10秒）
- 支持增量更新
- 支持强制刷新

#### 订单表：纯函数取模方案

**数据库路由**：`StoreIdDatabaseShardingAlgorithm`

```java
// 1. 计算 shard_id
int shardId = ShardUtil.calculateShardId(storeId);
// hash(store_id) & 1023

// 2. 直接取模
int dsIndex = shardId % 3;
String dsName = "ds" + dsIndex;
```

**表路由**：`StoreIdTableShardingAlgorithm`

```java
// 1. 计算 shard_id
int shardId = ShardUtil.calculateShardId(storeId);
// hash(store_id) & 1023

// 2. 直接取模
int tableIndex = shardId % 32;
String tableName = logicTableName + "_" + String.format("%02d", tableIndex);
```

**特点**：
- ❌ 无路由表
- ❌ 无缓存
- ❌ 无降级机制
- ✅ 纯函数计算，性能高

---

### 3. 扩容能力对比

#### 商品表：支持动态扩容

**扩容流程（3库 → 4库）**：

```
Step 1: 规划迁移的 bucket
  - 从 ds0/ds1/ds2 各选一部分 bucket 迁移到 ds3

Step 2: 更新路由表
  UPDATE product_shard_bucket_route 
  SET ds_name = 'ds3', status = 'MIGRATING', version = version + 1
  WHERE bucket_id IN (迁移的bucket列表);

Step 3: 数据迁移
  - 按 bucket_id 筛选数据
  - 从源库拷贝到目标库

Step 4: 切换路由
  UPDATE product_shard_bucket_route 
  SET status = 'NORMAL', version = version + 1
  WHERE bucket_id IN (迁移的bucket列表);

Step 5: 缓存自动刷新（10秒内）
  - ProductRouteCache 定时刷新
  - 或调用 forceRefresh() 立即刷新
```

**优势**：
- ✅ 无需修改代码
- ✅ 无需重启应用（缓存自动刷新）
- ✅ 只迁移部分 bucket，不是全量迁移
- ✅ 支持回滚（改回路由表即可）

#### 订单表：不支持动态扩容

**扩容流程（3库 → 4库）**：

```
Step 1: 修改代码
  - 修改 StoreIdDatabaseShardingAlgorithm
  - dsIndex = shardId % 4;  // 从 % 3 改为 % 4

Step 2: 重新部署
  - 重新编译打包
  - 重启所有应用实例

Step 3: 数据迁移
  - 全量迁移（因为取模算法改变，几乎所有数据都要迁移）

Step 4: 验证
  - 观察一段时间，确认无异常
```

**劣势**：
- ❌ 需要修改代码
- ❌ 需要重启应用
- ❌ 几乎全量迁移（取模算法改变）
- ❌ 回滚困难（需要再改代码）

---

### 4. 路由分布示例

#### 商品表：通过路由表映射（可配置）

**初始分布（3库，均匀分配）**：
```
bucket_id 范围      →  ds_name (路由表)  →  tbl_id (路由表)
─────────────────────────────────────────────────────────────
0-341              →  ds0              →  0-31
342-683            →  ds1              →  0-31
684-1023            →  ds2              →  0-31
```

**扩容后分布（4库，可灵活配置）**：
```
bucket_id 范围      →  ds_name (路由表)  →  tbl_id (路由表)
─────────────────────────────────────────────────────────────
0-255              →  ds0              →  0-31
256-511             →  ds1              →  0-31
512-767             →  ds2              →  0-31
768-1023            →  ds3              →  0-31
```

**热点调整（可配置）**：
```
bucket_id 范围      →  ds_name (路由表)  →  说明
─────────────────────────────────────────────────────────────
0-255              →  ds0              →  正常分布
256-511             →  ds1              →  正常分布
512-767             →  ds2              →  正常分布
768-1023            →  ds_hot           →  热点库（独立库）
```

#### 订单表：固定取模（不可配置）

**分布（固定）**：
```
bucket_id 范围      →  ds_name (取模)  →  tbl_id (取模)
─────────────────────────────────────────────────────────────
0, 3, 6, 9, ...    →  ds0              →  0, 32, 64, ...
1, 4, 7, 10, ...   →  ds1              →  1, 33, 65, ...
2, 5, 8, 11, ...   →  ds2              →  2, 34, 66, ...
```

**扩容后（需要修改代码）**：
```
bucket_id 范围      →  ds_name (取模)  →  tbl_id (取模)
─────────────────────────────────────────────────────────────
0, 4, 8, 12, ...   →  ds0              →  0, 32, 64, ...
1, 5, 9, 13, ...   →  ds1              →  1, 33, 65, ...
2, 6, 10, 14, ...  →  ds2              →  2, 34, 66, ...
3, 7, 11, 15, ...  →  ds3              →  3, 35, 67, ...
```

**问题**：
- ❌ 分布固定，无法调整
- ❌ 扩容时几乎全量迁移
- ❌ 无法处理热点（无法单独分配 bucket）

---

### 5. 性能对比

#### 商品表：路由表方案

**路由查询性能**：
- ✅ 内存缓存（`ProductRouteCache`），O(1) 查询
- ✅ 启动时全量加载，运行时无数据库查询
- ✅ 定时刷新（10秒），增量更新

**路由表查询开销**：
- 启动时：1次全量查询（1024条）
- 运行时：0次查询（使用缓存）
- 刷新时：1次增量查询（只查询变更记录）

#### 订单表：纯函数方案

**路由计算性能**：
- ✅ 纯函数计算，无数据库查询
- ✅ 无缓存开销
- ✅ 性能最优（直接计算）

**计算开销**：
- 每次路由：1次哈希计算 + 2次取模运算
- 无数据库查询
- 无缓存查询

---

### 6. 代码对比

#### 商品表：路由表方案

**配置**：
```java
// V2版本（使用路由表）
Properties dbShardingPropsV2 = new Properties();
dbShardingPropsV2.setProperty("algorithmClassName", 
    "com.jiaoyi.product.config.ProductShardIdDatabaseShardingAlgorithmV2");
dbShardingPropsV2.setProperty("use-routing-table", "true");
dbShardingPropsV2.setProperty("fallback-to-mod", "true");
```

**算法实现**：
```java
public class ProductShardIdDatabaseShardingAlgorithmV2 {
    private ProductRouteCache routeCache;  // 从Spring容器获取
    
    public String doSharding(...) {
        // 1. 从缓存查询路由
        String dsName = routeCache.getDataSourceName(productShardId);
        
        // 2. 如果缓存不可用，降级为取模
        if (dsName == null) {
            dsIndex = productShardId % 3;
            dsName = "ds" + dsIndex;
        }
        
        return dsName;
    }
}
```

#### 订单表：纯函数方案

**配置**：
```java
Properties dbShardingProps = new Properties();
dbShardingProps.setProperty("algorithmClassName", 
    "com.jiaoyi.order.config.StoreIdDatabaseShardingAlgorithm");
dbShardingProps.setProperty("ds-count", "3");
dbShardingProps.setProperty("ds-prefix", "ds");
```

**算法实现**：
```java
public class StoreIdDatabaseShardingAlgorithm {
    private int dsCount = 3;
    
    public String doSharding(...) {
        // 1. 计算 shard_id
        int shardId = ShardUtil.calculateShardId(storeId);
        
        // 2. 直接取模
        int dsIndex = shardId % dsCount;
        String dsName = "ds" + dsIndex;
        
        return dsName;
    }
}
```

---

## 🎯 为什么商品表使用路由表，订单表使用取模？

### 商品表的考虑

1. **需要频繁扩容**：商品数据增长快，需要灵活扩容
2. **需要热点调整**：某些店铺可能成为热点，需要单独分配
3. **需要迁移灵活性**：支持在线迁移，无需停机

### 订单表的考虑

1. **性能优先**：订单查询频繁，纯函数计算性能最优
2. **扩容频率低**：订单数据增长相对稳定
3. **简单可靠**：纯函数方案简单，无依赖，可靠性高

---

## 📝 建议

### 如果订单表也需要支持动态扩容

可以考虑将订单表也改为路由表方案：

1. **创建订单路由表**：`order_shard_bucket_route`
2. **创建路由缓存**：`OrderRouteCache`
3. **创建V2算法**：`StoreIdDatabaseShardingAlgorithmV2`
4. **迁移数据**：按 bucket 迁移

**优势**：
- ✅ 支持动态扩容
- ✅ 支持热点调整
- ✅ 与商品表保持一致

**劣势**：
- ❌ 增加复杂度
- ❌ 需要维护路由表
- ❌ 性能略低（缓存查询 vs 纯函数计算）

---

## 📊 总结

| 特性 | 商品表（路由表） | 订单表（取模） |
|------|----------------|---------------|
| **灵活性** | ✅ 高（可配置） | ❌ 低（固定） |
| **扩容能力** | ✅ 支持动态扩容 | ❌ 不支持 |
| **性能** | ✅ 高（缓存） | ✅ 最高（纯函数） |
| **复杂度** | ⚠️ 中等（路由表+缓存） | ✅ 低（纯函数） |
| **可靠性** | ✅ 高（有降级） | ✅ 高（无依赖） |
| **适用场景** | 需要频繁扩容、热点调整 | 性能优先、扩容频率低 |

**结论**：
- 商品表使用路由表方案，适合需要灵活扩容的场景
- 订单表使用取模方案，适合性能优先、扩容频率低的场景
- 两者都使用相同的虚拟桶算法（1024个桶），保证数据分布一致


