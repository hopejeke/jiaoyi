<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiaoyi.mapper.OrderItemMapper">

    <!-- 结果映射 -->
    <resultMap id="OrderItemResultMap" type="com.jiaoyi.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="subtotal" property="subtotal"/>
    </resultMap>

    <!-- 插入订单项 -->
    <insert id="insert" parameterType="com.jiaoyi.entity.OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_items (
            order_id, product_id, product_name, product_image, 
            unit_price, quantity, subtotal
        ) VALUES (
            #{orderId}, #{productId}, #{productName}, #{productImage},
            #{unitPrice}, #{quantity}, #{subtotal}
        )
    </insert>

    <!-- 批量插入订单项 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO order_items (
            order_id, product_id, product_name, product_image, 
            unit_price, quantity, subtotal
        ) VALUES
        <foreach collection="orderItems" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.productImage},
             #{item.unitPrice}, #{item.quantity}, #{item.subtotal})
        </foreach>
    </insert>

    <!-- 根据订单ID查询订单项列表 -->
    <select id="selectByOrderId" parameterType="long" resultMap="OrderItemResultMap">
        SELECT * FROM order_items WHERE order_id = #{orderId}
    </select>

    <!-- 根据商品ID查询订单项列表 -->
    <select id="selectByProductId" parameterType="long" resultMap="OrderItemResultMap">
        SELECT * FROM order_items WHERE product_id = #{productId}
    </select>

    <!-- 统计商品销售数量 -->
    <select id="sumQuantityByProductId" parameterType="long" resultType="long">
        SELECT COALESCE(SUM(quantity), 0) FROM order_items WHERE product_id = #{productId}
    </select>

</mapper>
